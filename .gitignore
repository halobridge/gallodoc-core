# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
*.egg
*.egg-info/
.eggs/

# Build artifacts
build/
dist/
wheels/
*.whl
*.tar.gz
.tox/
.cache/
.pytest_cache/
.coverage
.coverage.*
htmlcov/
coverage.xml
.mypy_cache/
.ruff_cache/
.pyre/

# Virtualenvs
.venv/
venv/
env/
ENV/

# Editor / IDE
.vscode/
.idea/
.DS_Store
*.swp
*.swo
.envrc

# Local secrets / config — must never be committed
.env
.env.*
!.env.example
*.pem
*.key
*.crt
*.p12
secrets.json
credentials.json

# Local notes / scratch
.notes/
SCRATCH.md
*.local.md

# Build distribution outputs
*.so

# Release safety gate report — generated by `make release-gate`.
# The script writes this each run; the contents are non-determining
# (path strings + counters) so we never commit it.
release_safety_report.json
